<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="aisato.ec.mapper.UserMapper">
    
    <resultMap id="UserResultMap" type="aisato.ec.entity.User">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="realName" column="real_name"/>
        <result property="gender" column="gender"/>
        <result property="birthDate" column="birth_date"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    
    <!-- ユーザー名またはメールアドレスでユーザーを検索 -->
    <select id="findByUsernameOrEmail" resultMap="UserResultMap">
        SELECT * FROM users 
        WHERE (username = #{usernameOrEmail} OR email = #{usernameOrEmail}) 
        AND status = 1
    </select>
    
    <!-- ユーザーIDでユーザーを検索 -->
    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE user_id = #{userId}
    </select>
    
    <!-- ユーザーを登録 -->
    <insert id="insert" parameterType="aisato.ec.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (
            username, password, email, phone, real_name, gender, birth_date, status
        ) VALUES (
            #{username}, #{password}, #{email}, #{phone}, #{realName}, #{gender}, #{birthDate}, #{status}
        )
    </insert>
    
    <!-- ユーザー情報を更新 -->
   <update id="update" parameterType="aisato.ec.entity.User">
    UPDATE users SET 
        phone = #{phone},
        real_name = #{realName},
        gender = #{gender},
        birth_date = #{birthDate},
        update_time = NOW()
    	WHERE user_id = #{userId}
	</update>
    
</mapper>